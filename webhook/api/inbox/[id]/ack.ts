import type { VercelRequest,VercelResponse } from "@vercel/node";import {sql} from "../../../lib/db.js";import {authorized} from "../../../lib/security.js";
export default async function handler(request:VercelRequest,response:VercelResponse){if(request.method!=="POST")return response.status(405).end();if(!authorized(request))return response.status(401).json({error:"Unauthorized"});const id=Array.isArray(request.query.id)?request.query.id[0]:request.query.id;if(!id)return response.status(400).json({error:"Missing id"});await sql()`UPDATE whatsapp_inbox SET acknowledged_at=COALESCE(acknowledged_at,NOW()) WHERE id=${id}`;return response.json({ok:true});}
